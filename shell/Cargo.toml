[package]
name = "racsh"
version.workspace = true
edition.workspace = true

[dependencies]
libc-lite = { path = "../libs/libc-lite" }

[dev-dependencies]
libc-lite = { path = "../libs/libc-lite", features = ["host-test"] }
