[package]
name = "racos-free"
version.workspace = true
edition.workspace = true

[[bin]]
name = "free"
path = "src/main.rs"

[dependencies]
libc-lite = { path = "../../../libs/libc-lite", features = ["alloc"] }
