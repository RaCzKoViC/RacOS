[package]
name = "racos-ping"
version.workspace = true
edition.workspace = true

[[bin]]
name = "ping"
path = "src/main.rs"

[dependencies]
libc-lite = { path = "../../../libs/libc-lite", features = ["alloc"] }
